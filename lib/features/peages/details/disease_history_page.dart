import 'package:flutter/material.dart';

class DiseaseHistory extends StatelessWidget {
  const DiseaseHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          color: Colors.black12,
          child: const Text(' Historia Chorobowa'),
        ),
      ),
    );
  }
}
